/**
 * Returns a list of CSS spacing properties.
 *
 * @return {Object} 				Margin and Padding specific attributes.
 */
const spacingAttrs = () => ( {
	top: '0px',
	left: '0px',
	right: '0px',
	bottom: '0px',
} );

export { spacingAttrs };
